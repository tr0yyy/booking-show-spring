export const ArchiveTicketsComponent = () => {
    return (
        <div>
            <p>Archiving tickets will cleanup tickets table of entries from past events, compressing ticket data into ticket_archive table.</p>
            <p> Are you sure you want to archive tickets from old events?</p>
        </div>
    )
}